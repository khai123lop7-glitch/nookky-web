# Branch sync note

`migration/v3-2-next` contains the active Next.js migration work. The branch had diverged from `main` only because `main` received temporary ZIP upload/delete commits. Those commits do not contain production source changes and should not overwrite the migration tree.
